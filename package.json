{
  "name": "4d",
  "version": "1.0.0",
  "scripts": {
    "watch:ts": "tsc --watch",
    "serve": "browser-sync start --server . --files \"dist/**/*.js, *.html\"",
    "dev": "concurrently \"npm run watch:ts\" \"npm run serve\"",
    "build": "tsc"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
